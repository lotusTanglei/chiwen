# Tasks

- [x] 任务 1：设计协作安全策略与参数
  - [x] 明确 init/sync 的默认策略（默认拒绝覆盖、支持 merge/overwrite、支持 fail-fast）
  - [x] 定义锁文件与状态元数据的文件名、结构字段与过期规则
  - [x] 定义 Git 工作区保护的检测点与开关参数

- [x] 任务 2：实现锁与状态元数据机制
  - [x] 在 init/sync 写入前后接入锁的获取与释放（包含异常路径释放）
  - [x] 写入/更新状态元数据（记录生成时间、版本、策略、修改文件清单）
  - [x] 在执行前读取状态并做风险检测（按策略 fail-fast 或显式允许）

- [x] 任务 3：降低多人协作下的 diff 与冲突
  - [x] 能力矩阵更新采用最小化 patch 策略（仅插入/降级必要行，避免整文件重写）
  - [x] 对新增能力项、分组顺序、插入位置做确定性规则与去重规则
  - [x] changelog 追加策略稳定化（同日多条记录的排序/去重，减少并发冲突概率）

- [x] 任务 4：对外接口与 Skill 编排更新
  - [x] 对 MCP 工具暴露必要的新参数（overwrite/merge、allow_dirty、allow_risky、lock_ttl 等）
  - [x] 更新 Skill：明确多人协作推荐流程与冲突处理指引

- [x] 任务 5：测试与验证
  - [x] 添加/更新单测：锁冲突、默认不覆盖、allow_dirty 分支、稳定排序与最小变更
  - [x] 验证：同一输入多次运行 diff 稳定（至少覆盖 `2_CAPABILITIES.md` 与 changelog）

# Task Dependencies
- 任务 2 依赖 任务 1
- 任务 3 依赖 任务 1
- 任务 4 依赖 任务 1
- 任务 5 依赖 任务 2、任务 3、任务 4
