- [x] init 在 `.docs/` 已存在时默认拒绝覆盖，并返回可执行的策略提示
- [x] sync 在写入 `.docs/` 前会获取锁，完成/异常路径会释放锁
- [x] 锁冲突时会 fail-fast，并返回包含持有者信息与处理建议的错误
- [x] 状态元数据在 init/sync 成功后会写入/更新，且包含版本、时间、策略与修改文件清单
- [x] sync 在检测到潜在覆盖风险时会按策略阻止或要求显式参数继续
- [x] 在 Git 仓库中，默认会对脏工作区 fail-fast，显式 allow 参数可继续并在结果里标记风险
- [x] `2_CAPABILITIES.md` 的新增/插入顺序稳定，重复运行不会产生无意义 diff
- [x] 能力矩阵更新为最小化变更（不整文件重写导致的行级大波动）
- [x] changelog 追加在同日多条记录下排序稳定、可去重，降低并发冲突概率
- [x] 单元测试覆盖：锁、默认不覆盖、allow_dirty/allow_risky 分支、稳定排序与最小变更
